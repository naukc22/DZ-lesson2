spring:
  shell:
    interactive:
      enabled: true
  datasource:
    url: jdbc:postgresql://localhost:5432/packages_db
    username: postgres
    password : postgres
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
